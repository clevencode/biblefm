import 'package:flutter/material.dart';

/// Cores principais do app (tema verde/preto).
abstract final class AppColors {
  /// Verde claro / amarelo-esverdeado (círculo do botão, destaque).
  static const Color lightGreen = Color(0xFFD4E157);
  /// Verde escuro (ícone play, contraste).
  static const Color darkGreen = Color(0xFF33691E);
  /// Amarelo-esverdeado pálido para o botão (como na referência).
  static const Color buttonCircle = Color(0xFFF0F4C3);
}
